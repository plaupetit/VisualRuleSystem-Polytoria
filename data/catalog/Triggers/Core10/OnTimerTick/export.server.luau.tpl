-- [VSR] Timer setup is represented by the graph Trigger.
-- [VSR] The generated Rule.Run function stays callable from manual Luau while the bridge grows.
vrsLog("Timer interval requested: ${param:interval} second(s).")
